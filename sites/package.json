{
  "name": "auto-citaciones-site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/write-indexnow-key.mjs",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
